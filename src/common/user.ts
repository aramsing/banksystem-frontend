export default interface User {
    userId?: number;
    username: string;
    password: string;
    email: string;
    firstname: string;
    lastname: string;
    address: string;
    usertype: string;
}

// ? means optional